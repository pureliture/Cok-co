import React from 'react';

const Reserve = () => {
  return (
    <div
      style={{
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
      }}
    >
      <div>Reserve</div>
    </div>
  );
};

export default Reserve;
