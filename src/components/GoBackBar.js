import { React } from 'react';
import './styles/GoBackBar.css';

const GoBackBar = () => {
  return (
    <div className="go_back_bar">
      <img className="left_arrow" src="/left_arrow.png"></img>
    </div>
  );
};

export default GoBackBar;
